---
target: current portfolio home page (app/page.tsx + live site)
total_score: 20
max_score: 40
na_heuristics: 
p0_count: 3
p1_count: 3
target_identity: "file:C:\\Users\\raroc\\OneDrive\\Desktop\\Personal Project\\my-portfolio\\my-portfolio\\app\\page.tsx"
target_fingerprint: "sha256:cf75cb6737171989e85d65668d58061652cdb4844b5a6d47ec4fe9347fc3594b"
target_path: "C:\\Users\\raroc\\OneDrive\\Desktop\\Personal Project\\my-portfolio\\my-portfolio\\app\\page.tsx"
timestamp: 2026-09-09T16-42-13Z
slug: app-page-tsx
---
Method: dual-agent (A: a2a743b5c0f400dc1 design review, B: a3b1f623754266d0a detector/browser) plus isolated technical audit agent (a3addd49d296b6136).

# Design Health Score

| # | Heuristic | Score | Key Issue |
|---|---|---|---|
| 1 | Visibility of System Status | 2 | Chat FAB green "online" dot bound to nothing; auto-carousels give no position/progress |
| 2 | Match System / Real World | 3 | "Tools & DevOps" holds Figma, VS Code, PayPal, 4 AI chat clients; cPanel URL shown to humans |
| 3 | User Control and Freedom | 1 | Two auto-players, neither pausable (WCAG 2.2.2); chat FAB undismissable; no nav on home |
| 4 | Consistency and Standards | 1 | Three H2 treatments, three container widths, two identical "View All" links to different places |
| 5 | Error Prevention | 2 | Good mailto fallback; no inline validation; staging URL ships as live project |
| 6 | Recognition Rather Than Recall | 2 | Filled/hollow timeline dots with no legend; infinite gallery with no end |
| 7 | Flexibility and Efficiency | 2 | Hero CTA row is a real fast path; no skip link, no section nav |
| 8 | Aesthetic and Minimalist Design | 2 | 35 skill pills, 4 generic testimonials, duplicated credentials sidebar, triple-animated FAB |
| 9 | Error Recovery | 3 | Strongest - toasts name the actual email as recovery path |
| 10 | Help and Documentation | 2 | Chat starter chips map 1:1 to sections already on screen |
| Total | | 20/40 | Acceptable - significant work needed |

Heuristics 7 and 10 were considered for n/a under Experience mode and deliberately scored: the chat widget is a real help layer and the hero CTA row a real efficiency path.

# Audit Health Score

| # | Dimension | Score | Key Finding |
|---|---|---|---|
| 1 | Accessibility | 1 | Two unpausable auto-players; gallery thumbs are div onClick (no keyboard); primary buttons 3.96:1 |
| 2 | Performance | 1 | setInterval(20ms) forcing ~50 sync reflows/sec; lightboxes serve 8.0 MB originals |
| 3 | Theming | 2 | Good oklch token system; 56 hard-coded colors; 2 components outside the system |
| 4 | Responsive | 2 | 8x8px dots, 10px hero text, fixed-height box clips at text zoom |
| 5 | Implementation Integrity | 1 | Reveal used in 2 of 9 sections; lib/data.ts duplicated into chat route and already drifted |
| Total | | 7/20 | Poor - major overhaul |

# Design Specificity Verdict

Category-interchangeable, while the underlying material is not. Composition is the 2024 default: centered max-w-4xl, dark mode, rounded-xl cards, pill tags, lucide icons, fade-up-on-scroll, fixed AI bubble. The eyebrow pattern is applied to 2 of 8 sections and set in a monospace stack with NO webfont loaded.

Core failure: gallery captions (lib/data.ts:264-267) contain the only non-interchangeable content on the site - an internal NFC card system built for BeachBus, a client-sponsored trip to El Nido, presenting at ERDC's Brokers Meeting. Rendered at text-xs inside opacity-0 group-hover overlays on an infinite auto-scroll strip under a heading that says "Gallery". Unreachable on touch. The NFC system appears nowhere in the projects array.

Deterministic scan: 1 finding total (bounce-easing, chat-widget.tsx:294, likely false positive - conventional typing-indicator idiom). The near-clean detector result is itself the finding: the problems are structural and editorial, not pattern-level. A linter cannot tell you your best story is in an alt attribute.

Agent disagreement resolved: A measured 15px horizontal overflow at 390px; B reproduced it, caught its own methodology error (desktop-rendered tree under a shrunken viewport), and after reload under proper emulation measured ZERO overflow. B is authoritative. A flagged muted-foreground as borderline; B and C independently measured 5.4:1 light and 7.4:1 dark - passes. Contrast failures are concentrated entirely in --primary.

# Priority Issues

[P0] Experience timeline is not chronological and contains middle-school coursework.
experience-section.tsx:15-18 sorts on the first 4-digit number in the date string. Renders 2024, 2025, 2024, 2023, 2023, 2020, 2018. Two roles overlap with no explanation. Last two entries are TVL-ICT Grade 7-8 strands at the same size and weight as employment. Fix: sort on parsed dates, delete high-school entries, split into labelled Work and Education groups.

[P0] The work section cannot prove the work.
Flagship card links to res326.servconfig.com/~graceland/staging/ and extractDomain renders that verbatim as the label. All descriptions line-clamp-2, truncating mid-sentence. Every card is an exit link; no case study, no detail route, no repo. links.github unused in all five featured projects. Thumbnails are full-page screenshots at ~290px, roughly 5px effective type, with zero differentiation between a WordPress site and a Next.js app. User has independently decided to abandon the mockups; the deeper fix is that cards must open INTO the portfolio.

[P0] Two unpausable auto-players and a keyboard-unreachable gallery.
gallery-section.tsx:90-98 setInterval(20ms); recommendations-section.tsx:26-32 6s timer. No pause control, no prefers-reduced-motion check. WCAG 2.2.2 Level A. gallery-section.tsx:152 thumbnails are div onClick with no role, tabIndex or key handler - all 6 images unreachable by keyboard, WCAG 2.1.1 Level A. The correct pattern already exists at image-lightbox.tsx:36-43 and was not reused.

[P1] Every primary button fails AA contrast.
White on #fd0058 = 3.96:1 (Lighthouse 3.94, agent B 3.95 - triangulated). Hits Resume CTA, Send Message, chat FAB, chat header, active filter pill. Light theme also drags eyebrow labels (3.96:1) and the hero achievement badge (3.34:1 at 10px). globals.css:19-20 already defines accent/accent-foreground at 7.13:1 and 10.58:1, unused.

[P1] The chat assistant describes projects that do not exist.
app/api/chat/route.ts imports lib/data.ts then hand-writes a second copy. Already diverged: the chatbot advertises Acad1, Muramart, ARWay and Enjoy Realty Redesign (absent from data.ts); the site ships Graceland, Naga Perks, UPCAT Review Plus, Downtown District Hotel, Azalea Main, Fish2Go and Online Creative Solutions (invisible to the chatbot).

[P1] Three routes, one title.
Home, /projects and /tech-stack all serve the same title tag. Both sub-pages open with 'use client' so neither can export metadata. /tech-stack has zero interactivity beyond a hover scale.

# SEO and AIEO measured state

robots.txt 404. sitemap.xml 404. llms.txt 404. favicon.ico 404 (no icon file in repo). Canonical absent on all routes. Zero JSON-LD blocks. Identical titles across 3 routes. One OG image site-wide. Content IS present in server HTML (SSG). Lighthouse: A11y 92, Best Practices 96, SEO 100, LCP 855ms, CLS 0.02.

Lighthouse SEO 100 is a trap: per-page audits do not check for a sitemap and treat a missing robots.txt as valid.

Highest-leverage AIEO work: JSON-LD is pure serialization from already-typed data (Person with knowsAbout, sameAs, alumniOf; WebSite; CreativeWork per project), plus llms.txt generated from lib/data.ts.

Sleeper finding: buildPortfolioContext() in the chat route is the most complete narrative account of the career anywhere in the repo and is reachable only by POSTing to /api/chat. Rendering it as real pages would roughly triple indexable surface area.

Caveat: ~35 elements ship with inline opacity:0 from framer-motion initial props. Rendering crawlers sampling pre-hydration, or visitors whose JS fails, see the name, Experience section and About copy invisible.

# Persona Red Flags

40-second recruiter screen: cannot answer "how many years"; Nov 2024-Present overlaps Mar-Aug 2025 unexplained; Grade 7-8 coursework in the employment rail; no years-of-experience statement, no availability; first project link reads res326.servconfig.com.

Engineering hiring manager: no GitHub in hero (one icon at page bottom); no case studies, every card exits; descriptions truncate before technical substance; Claude Code, Gemini CLI, Qoder/Qwen and Google AI Studio listed as peer skills alongside Git and Docker, inverting the intended signal; four structurally identical testimonials with no project, outcome or number; the shipped NFC system is absent from projects.

Keyboard and screen-reader user: 6 gallery images unreachable; chat panel has no role=dialog, focus trap, Escape or focus restoration; chat input has no accessible name; 8x8px carousel dots; no skip link; no nav on home; h1 serializes as "ChristianRaro" with no space.

# Minor Observations

Twitter-blue verification badge (hero-section.tsx:104-120) replicates Twitter's checkmark in exact #1DA1F2, the only literal hex in any component - cosplaying verification on a credibility surface. --primary-rgb is never defined, so the FAB glow falls back to rgb(220,38,38) against a #fd0058 button. The chat FAB runs three infinite animations plus a mount bounce. "View All" appears twice, identically styled, to different destinations, and /tech-stack shows the identical 35 pills. Three container widths and two H1 treatments across three pages. The /projects filter taxonomy mixes four technology categories with one industry category and two overlapping WordPress buckets. The lightbox bypasses next/image and serves 8.0 MB originals; the gallery folder totals 82 MB. 18 dead shadcn token declarations (--chart-1..5, --sidebar-*). achievements[0] is defined and never rendered. The resume.pdf console 404 is a Next RSC prefetch; the file itself serves 200.

# What's Working - preserve through redesign

1. The oklch token architecture (globals.css:5-100 plus tailwind.config.js with alpha-value support) is genuinely good. Do not rebuild it. Most theming failures are components bypassing it.
2. The contact form is the best-implemented surface: real labels, autoComplete, maxLength matching server LIMITS, independent server validation, HTML escaping, mailto degradation.
3. reveal.tsx:13-24 correctly diagnoses two framer-motion hydration hazards. The remedy for the motion findings is finishing its adoption, not designing something new.
4. Static prerendering across all routes keeps the AIEO story recoverable - the content is crawlable today.
5. Alt text is descriptive and contextual, valuable to screen readers and LLM crawlers alike.

# Questions to Consider

1. Why is the NFC card system in an alt attribute instead of being the first project?
2. "Claude Code, Gemini CLI, Qoder/Qwen, Google AI Studio" and "I care about clean maintainable code" cannot coexist credibly. Which claim is being made?
3. Every project card exits the site; the peak moment is the instant the visitor leaves. What would make them stay a fourth minute?
4. Is Graceland live somewhere real, or is the flagship a staging URL being sent to recruiters?
5. If the artifact is meant to lead, why is the chatbot the loudest thing on the page?
